{% comment %} Minimal page template - required for standard pages {% endcomment %}
<div class="page-content" style="max-width: 600px; margin: 0 auto; padding: 2rem var(--space);">
  <h1>{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</div>
